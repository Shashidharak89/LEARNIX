import { defineConfig } from "@trigger.dev/sdk/v3";

export default defineConfig({
  project: "proj_fulsbyjbycjetnzkbqqw",
  runtime: "node",
  logLevel: "info",
  triggerDirectories: ["./trigger"],
});
